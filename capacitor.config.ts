import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Woo_ionic_vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
